# Case Change Ledger

Every discovery-case change that alters a theorem, definition, metric, or case
mechanism must be recorded here.

| Date | Case | Change | Reason | Consequence |
| --- | --- | --- | --- | --- |
| 2026-07-29 | all | Initial discovery/confirmation separation created. | Prevent post-hoc case selection. | Discovery cases may shape theory; confirmation cases remain unfrozen. |
| 2026-07-29 | C5 | Marked `C5-independent-witness-v1` as non-final because `output_capacity=0` prevents real AGV dispatch. | Avoid overstating evidence from a minimal blocked-complete witness. | Final C5 must add AGV blocked-unload and hard-reservation comparison. |
| 2026-07-29 | C1/C2/C4 | Added explicit non-zero structural transitions for bounded LTS enumeration. | Make cycle/projection screens executable instead of static-only. | C1 keeps a cycle false positive with completion; C2 remains acyclic with completion; C4 full model blocks on AGV while its machine/buffer projection has progress. |
| 2026-07-29 | C5/C5_DAG | Kept `C5` as bidirectional blocking and added `C5_DAG` with the reverse route removed. | Provide the requested paired repair witness. | C5 has an initial closed blocking kernel; C5_DAG has no certificate and a marked completion path. |
| 2026-07-29 | C0-C5 | Added bounded stable-LTS enumeration with per-branch zero-time traces, all initial closure outcomes, frontier omissions and full-event shortest prefixes. | Close audit gaps caused by non-confluent closure and state-count truncation. | Reachable certificates now carry the shortest deterministic event prefix; truncated searches cannot claim exact supervisor completeness. |
| 2026-07-29 | C0/C1/C3/C4 | Added a capacity-aware state-dependent bipartite wait-graph `knot` baseline distinct from finite-LTS terminal SCCs. | Prevent the Palmer-style queueing baseline from being conflated with an IMS proof or a simple-cycle screen. | C0/C4 have capacity-closed knots; C1/C3 reject raw-cycle false positives caused by residual capacity or missing holders. |
| 2026-07-29 | BIX0 | Added deterministic candidate-state grid verification for capacities 1-3 and job counts 0-4. | Machine-check the P3c threshold without treating enumeration as proof. | Every checked point matches `n_A>=c_M and n_B>=c_G`; `c_D` invariance is confined to the no-transfer candidate prefix. |
| 2026-07-29 | CTMC fixtures | Added exact residuals, sensitivity equations, Doob-h generator checks and independent Gillespie streams with Wilson intervals. | Close the small-model probability audit while preserving rate provenance. | `C0/C1` remain `fixture_unverified`; no structural-case rate derivation or confirmation claim is made. |
| 2026-07-30 | BIX1-SAT/CE-BIXD1 | Added reachable empty-state BIX1-SAT family and classified persistent-D as outside the theorem. | Replace BIX0 reachability wording with an executable start/completion saturation theorem. | P3d threshold is `n_A>=c_M and n_B>=c_G`; truncated enumeration is not evidence; CE-BIXD1 is not a theorem mismatch. |
| 2026-07-30 | BIX2-PERSIST | Locked a 32-row ring/DAG discovery protocol before accepting grid results. | Make persistent `D` capacity causally material and attack every threshold dimension without using the intractable labeled `(2,2,2)` saturation point. | The family remains discovery-only; every mismatch or truncation must be retained, and it cannot enter the future confirmation set. |
| 2026-07-30 | G4 draft | Added literature-informed G4 case/preregistration design with CRP triad, recorder target-quantification obligation, and L30/B05 comparators. | Incorporate the completed seven-paper audit without changing theorem truth status or freezing confirmation results. | G4 becomes the next hard gate; all new families remain draft/not frozen until hashes, predictions, runtimes, and baseline manifests are committed. |
| 2026-07-30 | G4/G5 frozen panel | Executed exactly one `primary` and one `repro` capture for all nine sealed cases under G5-B commit `8aa752804b885b79e5371c98e7961087c540f2a8`; no retry or third run was made. | Close the first held-out panel without deleting deterministic failures. | Seven rows completed; grid and medium reproduced the same CTMC refusal because reachable states could not reach the declared `D union F` classes. The raw/hash evidence is retained and these two rows are `INCONCLUSIVE`. |
| 2026-07-30 | G5 scoring | Preserved the locked scorer output (`4 SUPPORTED / 3 FALSIFIED / 2 INCONCLUSIVE`) and added a transparent rule audit separating `theorem_prediction_correctness` from `certificate_minimality`. | The locked scorer incorrectly promoted two ancillary minimality failures into theorem-level falsifications even though the corresponding frozen falsifiers did not contain minimality. | The theorem-level audit is `6 SUPPORTED / 1 FALSIFIED / 2 INCONCLUSIVE`, with two separate `certificate_minimality=FAIL` results. The original output remains immutable evidence; scorer separation must be repaired before any successor freeze. |
| 2026-07-30 | G4 CRP/quantitative rows | Reclassified the failed mechanisms as successor discovery/regression evidence rather than modifying the sealed cases. | `G4_CRP_S4PR_AGREE` used a global certificate extractor for a declared local partial core; grid/medium left local-core terminal behavior outside the CTMC absorption taxonomy. | All G4 rows are retired from future held-out use. G6 must use synthetic regressions plus an independently generated and newly sealed confirmation set. |
| 2026-07-30 | G6-R1 historical replay | Retained the incomplete first replay after concurrent Windows schedule-state admission crossed the old fixed lease threshold. | Ordinary lock contention, occupied-sentinel inspection, and unsafe unlink retry were scheduler defects rather than scientific case failures. | R1 remains `FAILED_INCOMPLETE`; no missing row was backfilled and no scientific retry was introduced. |
| 2026-07-30 | G6-R2 historical replay | Retained five exactly matching primary/repro pairs and the failed mechanism summary. | Producer/solver accepted `1e-10` floating-point tolerance while G5/G6 consumers imposed exact `[0,1]`; `1.0000000000000002` was incorrectly classified as invalid. | R2 remains `FAILED_MECHANISM_CHECK`; the shared quantitative contract now audits the complete probability map, reported bounds, and residuals without clamping or rewriting R2. |
| 2026-07-30 | G6-R3 historical replay | Ran one preregistered primary and one repro for the same five retired historical rows under a fresh code tree, lock, and output root. | Verify the scheduler and quantitative-contract repairs without claiming a new held-out result. | All five raw/canonical/stderr pairs match and all five mechanism checks pass; two `certificate_minimality=FAIL/false` observations remain separate. R3 is regression evidence only and cannot satisfy G6-C/D/E. |
| 2026-07-30 | G6-B protocol foundation | Created an execution-disabled human protocol, five JSON protocol files, and a strict data-only validator; scientific execution performed=false and discovery cases created=false. | Lock ontology, independence, estimand, negative-control, and scoring layers before any discovery cases; review-found corrections distinguish `K_local` from `D_local`, case independence from metric-schema comparability, and remove the wrong-checkout cwd fallback. | G6-B remains `OPEN`; G6-C/D/E are `NOT STARTED`; no enumeration, CTMC, or DES run was performed; historical G4/G5/R1/R2/R3 evidence is unchanged. |
| 2026-07-31 | G6-B row-family protocol bundle | Implemented an exact-eight nested row-family bundle and data-only validator under `cases/discovery/g6b/row_families/structural_discovery_v1/`; canonical validation is valid but execution-disabled. | Make row-family identity, controlled reuse, overlap schema, later-lock schema, controls/probes, review state, and empty failure ledger machine-auditable before any case construction; independent reviews completed with specification `APPROVED`, code quality `APPROVED` with no Critical/Important/Minor blockers, and scientific boundary `PASS PROTOCOL ONLY`. | G6-B remains `OPEN`; G6-C/D/E are `NOT STARTED`; `case_creation_authorized=false`, `scientific_execution_authorized=false`, bundle `adversarial_review_status=PENDING`, actual overlap report, overlap-authority lock, execution-runtime lock, discovery outcomes, and output roots are absent, and historical G4/G5/R1/R2/R3 evidence is unchanged. |

| 2026-07-31 | G6-B ontology/absorption domain | Replaced the flat objective-class protocol language with typed selected/unselected/policy/derived groups and separated `S_reach` from certified `S_T`. | Correct the approved G6-B foundation before any case creation or science. | G6-B remains OPEN/PENDING; `case_creation_authorized=false`; `scientific_execution_authorized=false`; CE-NB1 and `tests/test_terminal_classes.py::test_branching_closed_class_separates_s_reach_from_s_t` define the boundary. |
