# G6-B Case Construction Plan Review

Date: 2026-08-02

Status: `APPROVED_BY_THREE_INDEPENDENT_REVIEWS / USER_APPROVAL_PENDING`

This record reviews one exact plan artifact. It does not authorize case
construction, retired-authority normalization, overlap execution,
target-certification preflight, CTMC, DES, quantitative execution, or a
scientific status change.

## Locked review subject

- Plan:
  `docs/superpowers/plans/2026-08-02-g6b-case-construction.md`.
- Exact plan SHA-256:
  `c20393328f8f98e7b35a6507fd6e993b2f17e068d977c3dc6253b05e9a75de5f`.
- Schema-contract remediation baseline:
  `b2f2f285a68793ce9ca4cb1b47a05dd7a3cfb9bb`.
- Approved design SHA-256:
  `b51b35848bec77ed787696a9c4fc88b4f299bde7368cc34380c2fc9143df3da6`.
- Frozen row-family matrix raw SHA-256:
  `487d81aa79a7bca681db81f19a4d6bd315668c43b1c4538c47f01f099d8e205f`.
- Final schema-review subject:
  `948ff5746adcb66b68fb9c47e519f070dd2c96ba`.

The plan has 13 case units, 26 method observations, and 13 exact/DES
companion groups. It includes all seven mandatory negative controls, a distinct
`D_global`-only positive, both `D_local` admission routes, one/multiple/zero CRP
kernel coverage, a multi-capacity residual boundary, exact/DES planned-target
pairing, and a fresh four-cell/two-AGV medium applicability probe. These are
planned discovery subjects, not results or confirmation evidence.

## Review history and closed findings

The first review round found:

- two HIGH authorization-validator defects in the publication branch; those
  were fixed separately at remediation subject `b2f2f285...`;
- a literal `estimand_id` schema contradiction; the plan now makes it an exact
  pre-case corrigendum task with only two predeclared allowed paths;
- an under-specified roster whose materializer would have had to invent
  scientific inputs;
- misuse of the no-double-count control as the only `D_global` positive;
- a validation command pointing to an unstated worktree;
- an incomplete exact-twelve change inventory;
- subject IDs in the DES stream derivation preimage;
- stale five-versus-six denominator wording;
- an R13 bypass claim that was not explicit in the recipe.

The final plan closes those findings by freezing:

- one approval-gated implementation worktree and one matching `PYTHONPATH`;
- three changed JSON contracts and nine byte-identical JSON contracts;
- an exact v2/v3/v2 estimand-scope correction contract;
- a normative R01-R13 recipe registry with closed state, route, parameter,
  rate, policy, target, control, prediction, metric, lineage, stream, path, and
  failure semantics;
- a separate `D_global`-only case;
- subject-free DES comparison projections;
- an explicit `route_delta_bypass` in R13;
- exact 13/26/13 manifests and stop conditions.

## Final independent verdicts

All three reviewers independently recomputed the exact plan SHA-256 above and
reviewed those same bytes.

| Review lane | Critical | High | Medium | Verdict |
| --- | ---: | ---: | ---: | --- |
| Ontology/estimand | 0 | 0 | 0 | `APPROVE` |
| Scientific boundary/nonreuse | 0 | 0 | 0 | `APPROVE` |
| Architecture/code/capability | 0 | 0 | 0 | `APPROVE` |

The ontology review confirmed case-versus-method counting, the
`D_global/D_local/F/R_livelock/R_terminal/P_policy` separation, planned versus
certified same-target identity, and 13/26/13 reconciliation. The scientific
review confirmed negative-control retention, subject-free random-stream
comparison, medium-island boundary wording, and the unchanged downstream gate
order. The architecture review confirmed the worktree/runtime lock, exact file
inventory, deterministic recipes, explicit write layout, and implementation
readiness.

## Mechanical plan checks

| Check | Result |
| --- | --- |
| SHA-256 recomputed independently by all three reviewers | matched |
| Markdown fence count | `40`, balanced |
| Trailing whitespace | none |
| Embedded NUL | absent |
| Plan status | `PROPOSED / PLAN-ONLY / AWAITING EXACT-BYTES USER APPROVAL` |

## Capability and claim boundary

The canonical current values remain:

```text
case_construction_authorized=false
retired_authority_fingerprint_normalization_authorized=false
target_certification_preflight_authorized=false
quantitative_execution_authorized=false
```

No case-unit or governance instance root has been created. No retired outcome
was read to design the recipes. No normalization, actual overlap audit,
Barrier-A preflight, target certificate, state enumeration, terminal
classification, CTMC, DES, output inspection, scoring, or status upgrade was
performed. G6-B remains `OPEN/PENDING`; G6-C/D/E remain unstarted.

The next transition requires the user to approve both the exact plan SHA-256
and this review artifact's exact SHA-256. Any plan-byte change invalidates these
three verdicts and requires a new review record.

